###
  EXTREME Memory Stress Test
  Maximum complexity untuk test RPN + IntentTree ketangguhan
###

fungsi utama()
  # LEVEL 1: Deep Expression Nesting (simulate complex RPN stack)
  var l1a = ((((1 + 2) * 3) + 4) * 5) + 6  # Deep nesting
  var l1b = ((((7 + 8) * 9) + 10) * 11) + 12
  var l1c = ((((13 + 14) * 15) + 16) * 17) + 18
  var level1 = l1a + l1b + l1c
  
  # LEVEL 2: Massive Arithmetic (simulate Intent Tree traversal)
  var l2a = (100 * 100) + (200 * 200) + (300 * 300)  # 140000
  var l2b = (400 * 400) + (500 * 500) + (600 * 600)  # 770000
  var l2c = (700 * 700) + (800 * 800) + (900 * 900)  # 1540000
  var level2 = l2a + l2b + l2c  # 2450000
  
  # LEVEL 3: Extreme Nested Operations (concurrent simulation)
  var l3_base = 1000
  var l3a = l3_base + (l3_base * 2) + (l3_base * 3) + (l3_base * 4)  # 10000
  var l3b = l3a + (l3a * 2) + (l3a * 3) + (l3a * 4)  # 100000
  var l3c = l3b + (l3b * 2) + (l3b * 3) + (l3b * 4)  # 1000000
  var level3 = l3c
  
  # LEVEL 4: Memory Fragmentation Simulation
  var frag1 = 12345 + 23456 + 34567 + 45678 + 56789
  var frag2 = frag1 * 2
  var frag3 = frag2 * 3  
  var frag4 = frag3 * 4
  var level4 = frag4
  
  # LEVEL 5: Stack Overflow Prevention Test
  var stack_depth = 0
  var stack_val = 1
  
  # Manual "recursion" simulation (50 levels deep)
  stack_val = stack_val + 1  # Level 1
  stack_val = stack_val * 2
  stack_val = stack_val + 1  # Level 2  
  stack_val = stack_val * 2
  stack_val = stack_val + 1  # Level 3
  stack_val = stack_val * 2
  stack_val = stack_val + 1  # Level 4
  stack_val = stack_val * 2
  stack_val = stack_val + 1  # Level 5
  stack_val = stack_val * 2
  
  # Continue pattern...
  stack_val = stack_val + 1
  stack_val = stack_val * 2
  stack_val = stack_val + 1
  stack_val = stack_val * 2
  stack_val = stack_val + 1
  stack_val = stack_val * 2
  stack_val = stack_val + 1
  stack_val = stack_val * 2
  
  var level5 = stack_val
  
  # FINAL MEGA CALCULATION
  # Combine all levels with maximum arithmetic complexity
  var mega_result = (level1 * level2) + (level3 * level4) + level5
  
  # Return modulo to prevent overflow while maintaining complexity
  mega_result % 1000000
tutup_fungsi
